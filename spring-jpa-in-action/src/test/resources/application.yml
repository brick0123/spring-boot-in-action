spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 1000
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:tc:mysql:5.7.34://test-container
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
